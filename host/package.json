{
  "name": "@ozmux/host",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --format=esm --outfile=../assets/host.mjs",
    "check-types": "tsc --noEmit",
    "test": "vitest run"
  },
  "packageManager": "pnpm@10.30.2",
  "devDependencies": {
    "@types/node": "catalog:",
    "esbuild": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
