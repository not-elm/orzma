//! Per-activity page handle: wraps a `chromiumoxide::Page`, owns the
//! screencast subscription task, and exposes navigate/resize/input entry
//! points. Filled in by Task 2.7.

// Subsequent tasks (2.7) populate this module.
